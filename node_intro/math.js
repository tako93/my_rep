 const add = (a, b) => a + b;
 const minus = (a, b) => a - b;
 const multiply = (a, b) => a * b;
 const devide = (a, b) => a / b;



// console.log(add(87, 89))

export default {
    title: 'math library',
};